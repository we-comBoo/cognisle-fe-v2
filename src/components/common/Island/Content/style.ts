import styled from '@emotion/styled'

const Layout = {
  StyledRoot: styled.div`
    position: relative;
    height: 78rem;
    width: 43rem;
  `,
}
const Items = {
  StyledRoot: styled.div`
    position: relative;
    top: 0;
    left: 0;
    height: inherit;
  `,
}

export { Layout, Items }
