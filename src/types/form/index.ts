import { loginProps, loginValidationProps } from './login'
import { signupProps, signupValidationProps } from './signup'

export type {
  signupProps,
  signupValidationProps,
  loginProps,
  loginValidationProps,
}
