interface PointBtnInfoProps {
  size: {
    width: number
    height?: number
  }
  point: {
    width: number
    height?: number
  }
  color: {
    main: string
    point: string
  }
  round: boolean
}
export type { PointBtnInfoProps }
